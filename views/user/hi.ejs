

<style>
    body{margin-top:20px;
background-color:rgb(225, 235, 225)
    }

.card {
    margin-bottom: 24px;
    -webkit-box-shadow: 0 2px 3px white;
    box-shadow: 0 2px 3px white
}
.card {
    position: relative;
    display: -webkit-box;
    display: -ms-flexbox;
    display: flex;
    -webkit-box-orient: vertical;
    -webkit-box-direction: normal;
    -ms-flex-direction: column;
    flex-direction: column;
    min-width: 0;
    word-wrap: break-word;
    background-color:white;
    background-clip: border-box;
    border: 1px solid rgb(26, 83, 25);
    border-radius: 1rem;
}
.activity-checkout {
    list-style: none
}

.activity-checkout .checkout-icon {
    position: absolute;
    top: -4px;
    left: -24px
}

.activity-checkout .checkout-item {
    position: relative;
    padding-bottom: 24px;
    padding-left: 35px;
    border-left: 2px solid rgb(26, 83, 25);
}

.activity-checkout .checkout-item:first-child {
    border-color:rgb(26, 83, 25);
}

.activity-checkout .checkout-item:first-child:after {
    background-color: rgb(26, 83, 25);
}

.activity-checkout .checkout-item:last-child {
    border-color: transparent
}

.activity-checkout .checkout-item.crypto-activity {
    margin-left: 50px
}

.activity-checkout .checkout-item .crypto-date {
    position: absolute;
    top: 3px;
    left: -65px
}



.avatar-xs {
    height: 1rem;
    width: 1rem
}

.avatar-sm {
    height: 2rem;
    width: 2rem
}

.avatar {
    height: 3rem;
    width: 3rem
}

.avatar-md {
    height: 4rem;
    width: 4rem
}

.avatar-lg {
    height: 5rem;
    width: 5rem
}

.avatar-xl {
    height: 6rem;
    width: 6rem
}

.avatar-title {
    -webkit-box-align: center;
    -ms-flex-align: center;
    align-items: center;
    background-color:  #80AF81;
    color: rgb(26, 83, 25);
    display: -webkit-box;
    display: -ms-flexbox;
    display: flex;
    font-weight: 500;
    height: 100%;
    -webkit-box-pack: center;
    -ms-flex-pack: center;
    justify-content: center;
    width: 100%
}

.avatar-group {
    display: -webkit-box;
    display: -ms-flexbox;
    display: flex;
    -ms-flex-wrap: wrap;
    flex-wrap: wrap;
    padding-left: 8px
}

.avatar-group .avatar-group-item {
    margin-left: -8px;
    border: 2px solid rgb(26, 83, 25);
    border-radius: 50%;
    -webkit-transition: all .2s;
    transition: all .2s
}

.avatar-group .avatar-group-item:hover {
    background-color: #80AF81;
    position: relative;
    -webkit-transform: translateY(-2px);
    transform: translateY(-2px)
}

.card-radio {
    background-color: #fff;
    border: 2px solid rgb(26, 83, 25);
    border-radius: .75rem;
    padding: .5rem;
    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap;
    display: block
}

.card-radio:hover {
    cursor: pointer
}

.card-radio-label {
    display: block
}

.edit-btn {
    width: 35px;
    height: 35px;
    line-height: 40px;
    text-align: center;
    position: absolute;
    right: 25px;
    margin-top: -50px
}

.card-radio-input {
    display: none
}

.card-radio-input:checked+.card-radio {
    border-color: rgb(26, 83, 25)
}


.font-size-16 {
    font-size: 16px!important;
}
.text-truncate {
    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap;
}

a {
    text-decoration: none!important;
}


.form-control {
    display: block;
    width: 100%;
    padding: 0.47rem 0.75rem;
    font-size: .875rem;
    font-weight: 400;
    line-height: 1.5;
    color:rgb(26, 83, 25);
    border-radius: .75rem;
    background-color: #fff;
    background-clip: padding-box;
    border: 1px solid rgb(26, 83, 25);
    -webkit-appearance: none;
    -moz-appearance: none;
    appearance: none;
    border-radius: 0.75rem;
    -webkit-transition: border-color .15s ease-in-out,-webkit-box-shadow .15s ease-in-out;
    transition: border-color .15s ease-in-out,-webkit-box-shadow .15s ease-in-out;
    transition: border-color .15s ease-in-out,box-shadow .15s ease-in-out;
    transition: border-color .15s ease-in-out,box-shadow .15s ease-in-out,-webkit-box-shadow .15s ease-in-out;
}

.edit-btn {
    width: 35px;
    height: 35px;
    line-height: 40px;
    text-align: center;
    position: absolute;
    right: 25px;
    margin-top: -50px;
}

.ribbon {
    position: absolute;
    right: -26px;
    top: 20px;
    -webkit-transform: rotate(45deg);
    transform: rotate(45deg);
    color:rgb(26, 83, 25);
    font-size: 13px;
    font-weight: 500;
    padding: 1px 22px;
    font-size: 13px;
    font-weight: 500
}


/* Hover state - changes background and text color to indicate selection */
.card-radio:hover {
    background-color: #e0f7e9; /* Light green background */
    color: white;           /* Green text */
    border-color: #008000;    /* Green border */
}

/* Active state - when address is clicked, this will persist the green color */
.card-radio.active {
    background-color:rgb(128, 175, 129);/* Solid green background */
    color: white;              /* White text */
    border-color: #008000;     /* Green border */
}

body{
    background:#eee;
}

.card {
    box-shadow: 0 20px 27px 0 rgb(0 0 0 / 5%);
}

.card {
    position: relative;
    display: flex;
    flex-direction: column;
    min-width: 0;
    word-wrap: break-word;
    background-color: #fff;
    background-clip: border-box;
    border: 0 solid rgba(0,0,0,.125);
    border-radius: 1rem;
}

.card-body {
    -webkit-box-flex: 1;
    -ms-flex: 1 1 auto;
    flex: 1 1 auto;
    padding: 1.5rem 1.5rem;
}


</style>





    <link href="https://stackpath.bootstrapcdn.com/bootstrap/5.1.0/css/bootstrap.min.css" rel="stylesheet">

    <div class="container checkout-container">
        <div class="checkout-wrapper">
            <!-- Order Summary Section -->
            <div class="order-summary mb-4">
                <div class="card" style="border-color: rgb(80, 141, 78);">
                    <div class="card-body">
                        <!-- Order Summary Header -->
                        <div class="p-3 mb-3">
                            <h5 class="font-size-16 mb-0" style="color: rgb(26, 83, 25);">Order Summary
                                <span class="float-end ms-2" style="color: rgb(26, 83, 25);">#MN0124</span>
                            </h5>
                        </div>
    
                        <!-- Order Summary Table -->
                        <div class="table-responsive">
                            <table class="table table-centered mb-0 table-nowrap">
                                <thead>
                                    <tr>
                                        <th style="width: 110px;">Product</th>
                                        <th>Product Desc</th>
                                        <th>Price</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    <% cartDetails.items.forEach(item => { %>
                                    <tr>
                                        <th>
                                            <img src="<%= item.productImage %>" alt="<%= item.productId.name %>" class="avatar-lg rounded" style="max-width: 100px; height: 100px; object-fit: cover;">
                                        </th>
                                        <td>
                                            <h5 class="font-size-16 text-truncate"><%= item.productId.name %></h5>
                                            <p class="text-muted mb-0"><%= item.productPrice.toFixed(2) %> x <%= item.productCount %></p>
                                        </td>
                                        <td>$<%= (item.productPrice * item.productCount).toFixed(2) %></td>
                                    </tr>
                                    <% }) %>
                                    <tr class="bg-light">
                                        <td colspan="2">
                                            <h5 class="font-size-14 m-0">Total:</h5>
                                        </td>
                                        <td>$<%= cartDetails.items.reduce((total, item) => total + (item.productPrice * item.productCount), 0).toFixed(2) %></td>
                                    </tr>
                                </tbody>
                            </table>
                        </div>
    
                        <!-- Order Total Section -->
                        <div class="p-3 mt-3">
                            <h6 class="card-title mb-3">Order Total</h6>
                            <div class="d-flex justify-content-between mb-1 small">
                                <span>Subtotal</span> 
                                <span id="payableAmount">$<%= cartDetails.items.reduce((total, item) => total + (item.productPrice * item.productCount), 0)%></span>
                            </div>
                            <div class="d-flex justify-content-between mb-1 small">
                                <span>Coupon 
                                    <input type="text" id="couponCodeInput" placeholder="Enter Coupon Code" class="form-control form-control-sm d-inline-block w-auto mx-2" />
                                    <button class="btn btn-link btn-sm p-0" id="applyCouponButton" style="color: rgb(26, 83, 25);">Apply Coupon</button>
                                    <button class="btn btn-sm d-none" id="removeCouponButton" title="Remove Coupon">
                                        ✖
                                    </button>
                                </span>
                            </div>
    
                            <hr>
                        </div>
                    </div>
                </div>
            </div>
    
            <!-- Checkout Form Section -->
            <div class="checkout-details">
                <div class="card border-0">
                    <div class="card-body">
                       
                            <ol class="activity-checkout mb-0 px-4 mt-3">
                                <!-- Address Section -->
                                <li class="checkout-item">
                                    <div class="avatar checkout-icon p-1">
                                        <div class="avatar-title rounded-circle bg-success">
                                            <i class="bx bxs-truck text-white font-size-20"></i>
                                        </div>
                                    </div>
                                    <div class="feed-item-list">
                                        <div>
                                            <h5 class="font-size-16 mb-1">Shipping Info</h5>
                                            <form id="orderForm"  action="/place-order" method="POST">
                                            <div class="mb-5">
                                                <div class="row">
                                                    <div class="col-lg-12">
                                                        <div>
                                                            <span class="fs-14 mb-4 d-block">Saved Address</span>
                                                            <% if (addresses.length > 0) { %>
                                                                <% addresses.forEach(address => { %>
                                                                    <label class="card-radio-label mb-3">
                                                                        <input type="radio" name="address" class="card-radio-input" value="<%= address._id %>" required>
                                                                        <div class="card-radio p-3" data-address-id="<%= address._id %>">
                                                                            <div style="color:rgb(26, 83, 25);">
                                                                                <p><%= address.building %>, <%= address.street %>, <%= address.city %>, <%= address.country %>, <%= address.postalCode %></p>
                                                                            </div>
                                                                        </div>
                                                                    </label>
                                                                <% }) %>
                                                            <% } else { %>
                                                                <p>No addresses available. Please add a new address.</p>
                                                            <% } %>
                                                        </div>
                                                        
                                                        <script>
                                                            // Add click event listener to all address cards
                                                            document.querySelectorAll('.card-radio-label').forEach(card => {
                                                                card.addEventListener('click', function () {
                                                                    // Remove active class from all cards
                                                                    document.querySelectorAll('.card-radio').forEach(c => c.classList.remove('active'));
                                                        
                                                                    // Add active class to the clicked card
                                                                    this.querySelector('.card-radio').classList.add('active');
                                                                });
                                                            });
                                                        </script>
                                                        
                                                        <!-- Change Button -->
                                                        <div class="mt-3">
                                                            <a href="/userprofile" class="btn btn-success">Change</a>
                                                        </div>
                                                    </div>
                                                </div>
                                            </div>
                                
                                        </div>
                                    </div>
                                </li>
    
                                <!-- Payment Method Section -->
                                <li class="checkout-item">
                                    <div class="avatar checkout-icon p-1">
                                        <div class="avatar-title rounded-circle bg-success">
                                            <i class="bx bxs-wallet-alt text-white font-size-20"></i>
                                        </div>
                                    </div>
                                    <div class="feed-item-list">
                                        <div>
                                            <h5 class="font-size-14 mb-3">Payment method</h5>
                                            <div class="row">
                                                <div class="col-lg-4 col-sm-6 mb-3">
                                                    <label class="card-radio-label">
                                                        <input type="radio" name="paymentMethod" value="Wallet" class="card-radio-input"  required>
                                                        <span class="card-radio py-3 text-center text-truncate">
                                                            <i class="bx bx-wallet d-block h2 mb-3"></i>
                                                            Wallet
                                                        </span>
                                                    </label>
                                                </div>
                                                <div class="col-lg-4 col-sm-6 mb-3">
                                                    <label class="card-radio-label">
                                                        <input type="radio" name="paymentMethod" value="Razorpay" class="card-radio-input" id="razorpayOption" required>
                                                        <span class="card-radio py-3 text-center text-truncate">
                                                            <i class="bx bx-rupee d-block h2 mb-3"></i>
                                                            Razorpay
                                                        </span>
                                                    </label>
                                                </div>
                                                <div class="col-lg-4 col-sm-6 mb-3">
                                                    <label class="card-radio-label">
                                                        <input type="radio" name="paymentMethod" value="Cash on Delivery" class="card-radio-input" required checked>
                                                        <span class="card-radio py-3 text-center text-truncate">
                                                            <i class="bx bx-money d-block h2 mb-3"></i>
                                                            Cash on Delivery
                                                        </span>
                                                    </label>
                                                </div>
                                            </div>
                                        </div>
                                    </div>
                                </li>
                            </ol>
                            <!-- Actions Section -->
                            <div class="row my-4">
                                <div class="col">
                                    <a href="/cart" class="btn btn-success">
                                        <i class="mdi mdi-arrow-left me-1"></i> Continue Shopping
                                    </a>
                                </div>
                                <div class="col">
                                    <div class="text-end mt-2 mt-sm-0">
                                        <button type="submit" class="btn btn-success">
                                            <i class="mdi mdi-cart-outline me-1"></i> Place Order
                                        </button>
                                    </div>
                                </div>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>
    
    <!-- Bootstrap JS and jQuery -->
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/5.1.0/js/bootstrap.bundle.min.js"></script>
    
    <!-- Optional: Coupon Modal from original code -->
    <div class="modal fade" id="couponModal" tabindex="-1" aria-labelledby="couponModalLabel" aria-hidden="true">
        <div class="modal-dialog modal-lg">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title" id="couponModalLabel">Available Coupons</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <div class="modal-body">
                    <div id="couponList"></div>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Close</button>
                </div>
            </div>
        </div>
    </div>



<script src="https://checkout.razorpay.com/v1/checkout.js"></script>

<!-- Include jQuery -->
<script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>


<!-- SweetAlert CDN -->
<script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>

    <script>
    $(document).ready(function () {
        // When the "Show Coupon Code" button is clicked
        $('#applyCouponButton').click(function () {
            // Open the modal to show the coupon list
            $('#couponModal').modal('show');

            // Fetch coupon data from the server
            $.ajax({
                url: '/get-coupons', // Server route to fetch coupons
                method: 'GET',
                success: function (response) {
                    if (response.success) {
                        // Populate modal with coupon data
                        let couponHTML = '';
                        response.coupons.forEach(function (coupon) {
                            couponHTML += `
                                <div class="coupon-item mb-3">
                                    <p><strong>Code:</strong> ${coupon.code}</p>
                                    <p><strong>SAVE:</strong> $${coupon.discountValue}</p>
                                    <p><strong>End Date:</strong> ${new Date(coupon.endDate).toLocaleDateString()}</p>
                                    <p><strong>Status:</strong> ${coupon.isActive ? 'Active' : 'Inactive'}</p>
                                    <button class="btn btn-success apply-coupon-btn" data-coupon-code="${coupon.code}" style="color: white;">Apply Coupon</button>
                                    <hr>
                                </div>
                            `;
                        });
                        $('#couponList').html(couponHTML);  // Assuming you have a div with id "couponList" to display the coupons
                    } else {
                        alert('Failed to load coupon data.');
                    }
                },
                error: function () {
                    alert('Error fetching coupon data.');
                }
            });
        });

        // Event listener for "Apply Coupon" buttons
        $(document).on('click', '.apply-coupon-btn', function () {
            // Get the coupon code from the button's data attribute
            const couponCode = $(this).data('coupon-code');

            // Update the placeholder of the coupon input field with the selected coupon code
            $('#couponCodeInput').attr('placeholder', couponCode);

            // Send a POST request to apply the coupon
            $.ajax({
                type: 'POST',
                url: '/apply-coupon',
                data: { couponCode: couponCode },
                success: function (response) {
                    if (response.success) {
                        // Update the UI with the discount and new total
                        $('#couponDiscount').text(`-$${response.discount.toFixed(2)}`);
                        $('#payableAmount').text(`$${response.newTotal.toFixed(2)}`);

                        // Show the close button
                        $('#removeCouponButton').removeClass('d-none');
                        alert('Coupon applied successfully!');
                    } else {
                        alert(response.message);
                    }
                },
                error: function () {
                    alert('Error applying coupon.');
                }
            });

            // Optionally, close the modal after applying the coupon
            $('#couponModal').modal('hide');
        });

        // Remove the applied coupon
        $('#removeCouponButton').click(function () {
            $.ajax({
                type: 'POST',
                url: '/remove-coupon',
                success: function (response) {
                    if (response.success) {
                        // Reset discount and payable amount
                        $('#couponDiscount').text('-$0.00');
                        $('#payableAmount').text(`$${response.originalTotal.toFixed(2)}`);

                        // Hide the close button
                        $('#removeCouponButton').addClass('d-none');

                        alert('Coupon removed successfully!');
                    } else {
                        alert(response.message);
                    }
                },
                error: function () {
                    alert('Error removing coupon.');
                }
            });
        });

});

</script>
